const arr = [3,5,6,7]
arr = 'ram'
console.log(arr)

//map vs foreach vs filter 

// ->> Map can transform the Elements
// ->> forEach displays the wanted or selected element only 
// ->> filter is used to separate and display the elements that satisfies certain conditons
